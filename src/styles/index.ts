import { createStitches } from '@stitches/react'

export const {
  styled, config, globalCss, keyframes, getCssText, theme, createTheme, css
} = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6'
    }
  }
})
